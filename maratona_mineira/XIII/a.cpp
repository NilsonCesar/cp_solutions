#include <bits/stdc++.h>

using namespace std;

#define _ cin.tie(0); cout.tie(0); ios_base::sync_with_stdio(false);

int main(void) {_
    int x;
    cin >> x;
    cout << 2 * x + ((x + 1) * x) / 2 << '\n';

    return 0;
}